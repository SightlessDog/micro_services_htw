# Copy to .env for local `npm run dev` (Docker build reads these via build args instead).
VITE_ZITADEL_ISSUER=http://localhost:8081
VITE_ZITADEL_CLIENT_ID=change-me
# Org ID that owns the SPA client above — pins login/registration to that org.
VITE_ZITADEL_ORG_ID=change-me
